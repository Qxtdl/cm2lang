fn noreturn main() {
   v16 x = 42;
   v16 y = 69;
   v16 z = 0;
   v16 operator = 0;
   // Operator:
   // 0: +
   // 1: -
   if (operator == 0) {
      x = x + y + z;
   }
   if (operator == 1) {
      x = x - y - z;
   }
   asm "done:"
   asm "jmp done"
}