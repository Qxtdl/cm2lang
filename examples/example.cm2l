fn noreturn main(noparam) {
    v16 loopcounter = 0;
    v16 x = loopcounter;
    v16 y = 1;
    asm "fibloop:"
    x = x + y;
    y = y + x;
    loopcounter = loopcounter + 1;
    if (loopcounter == 1) {asm "jmp main"}
    asm "jmp fibloop"
}